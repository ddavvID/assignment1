print("請設計一個函數，輸入為數字，輸出則為*符號為輸入數字邊長的空心正三角形。")
rows = int (input ("輸入："))
for i in range (rows):
  if (i==rows-1):
    for y in range (2*rows-1):
        if (y%2==0):
            print ("*"),
        else:
            print (""),
    print ("\n")

  else:
    
    for p in range (2*rows-1):
        if ((p==(2*rows-1)//2-i) or (p==(2*rows-1)//2+i)):
             print ("*"),
        else:
             print (""),
    print ("\n")
        
